import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.serenephoenixbuzz',
  appName: 'serene-phoenix-buzz',
  webDir: 'dist'
};

export default config;
